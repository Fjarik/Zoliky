@using ZolikyWeb.Tools
@model IEnumerable<DataAccess.Models.Transaction>


<div style="width: 100%">
	<ol class="timeline">
		@foreach (var trans in Model) {
			<li class="timeline__step done">
				<input class="timeline__step-radio" id="@trans.ID" name="Test" type="radio">

				@*<label class="timeline__step-label" for="@trans.ID">
					<span class="timeline__step-content">
						@trans.Date.ToShortDateString()
					</span>
				</label>*@

				<span class="timeline__step-title">
					@trans.From
				</span>

				<img class="timeline__step-marker" src="@Url.GetProfilePhotoUrl(trans.FromID)"/>
			</li>
		}
	</ol>
</div>

@*<section class="cd-horizontal-timeline">
	<div class="timeline">
		<div class="events-wrapper">
			<div class="events">
				<ol>
					@foreach (var trans in Model) {
						<li>
							<a href="#0" data-date="@(trans.Date.ToString("dd/MM/YYYY"))" class="@(trans.ID == Model.First().ID ? "selected" : "")">
								@trans.From
							</a>
						</li>
					}
				</ol>
				<span class="filling-line" aria-hidden="true"></span>
			</div>
			<!-- .events -->
		</div>
		<!-- .events-wrapper -->
		<ul class="cd-timeline-navigation">
			<li>
				<a href="#0" class="prev inactive">Prev</a>
			</li>
			<li>
				<a href="#0" class="next">Next</a>
			</li>
		</ul>
		<!-- .cd-timeline-navigation -->
	</div>
	<!-- .timeline -->
	<div class="events-content">
		<ol>
			@foreach (var trans in Model) {
				<li data-date="@(trans.Date.ToString("dd/MM/YYYY"))" class="@(trans.ID == Model.First().ID ? "selected" : "")">
					<h2>
						<img class="img-circle pull-left m-r-20 m-b-10" width="60" alt="user" src="@Url.GetProfilePhotoUrl(trans.FromID)" />
					</h2>
					<br />
					<small>@trans.Date.ToShortDateString()</small>
					<hr class="m-t-40" />
					<p class="m-t-40">
						@trans.From
					</p>
				</li>
			}
		</ol>
	</div>
	<!-- .events-content -->
</section>*@